<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include
        android:id="@+id/toolBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        layout="@layout/tool_bar" />

    <RelativeLayout
        android:id="@+id/contentLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/find"
        android:layout_below="@id/toolBar"
        android:background="#FFFFFF" >

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true" >

            <Button
                android:id="@+id/goToIdentify"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:background="@drawable/border_button_selector"
                android:minHeight="60dp"
                android:paddingLeft="80dp"
                android:paddingRight="80dp"
                android:text="@string/go_to_identify"
                android:textColor="@color/yellow_text_selector"
                android:textSize="22sp" />

            <Button
                android:id="@+id/goToFind"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/goToIdentify"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="35dp"
                android:background="@drawable/border_button_selector"
                android:minHeight="60dp"
                android:minWidth="250dp"
                android:paddingLeft="70dp"
                android:paddingRight="70dp"
                android:text="@string/go_to_find"
                android:textColor="@color/yellow_text_selector"
                android:textSize="22sp" />
        </RelativeLayout>
    </RelativeLayout>

</RelativeLayout>